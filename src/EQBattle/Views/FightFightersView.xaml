<UserControl x:Class="EQBattle.Views.FightFightersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EQBattle.Views"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="500">
  <Grid DataContext="{Binding Fight}">
    <ListView x:Name="lvFighters" SelectionMode="Single" ItemsSource="{Binding Fighters}">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
          <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
      </ListView.ItemContainerStyle>
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Name" Width="60" DisplayMemberBinding="{Binding Character.Name}"/>
          <GridViewColumn Header="Time" Width="60" DisplayMemberBinding="{Binding LogLine.LogTime, StringFormat=HH\\:mm\\:ss}"/>
          <GridViewColumn Header="Message" Width="60" DisplayMemberBinding="{Binding LogLine.LogMessage}"/>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>
