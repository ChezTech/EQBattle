<UserControl x:Class="EQBattle.Views.BattleHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EQBattle.Views"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <ToolBarTray IsLocked="True">
            <ToolBar>
                <Button Content="Open" Command="{Binding Open}"/>
            </ToolBar>
            <ToolBar>
                <Button Content="Run" Command="{Binding Run}"/>
                <Button Content="Pause" Command="{Binding Pause}"/>
            </ToolBar>
        </ToolBarTray>
    </Grid>
</UserControl>
