<UserControl x:Class="EQBattle.Views.BattleHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EQBattle.Views"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <ToolBarTray IsLocked="True">
            <ToolBar Loaded="ToolBar_Loaded">
                <Button Content="Open" Command="{Binding Open}" ToolBar.OverflowMode="Never"/>
            </ToolBar>
            <ToolBar Loaded="ToolBar_Loaded">
                <Button Content="Run" Command="{Binding Run}" ToolBar.OverflowMode="Never"/>
                <Button Content="Pause" Command="{Binding Pause}" ToolBar.OverflowMode="Never"/>
                <Button Content="Refresh" Command="{Binding Refresh}" ToolBar.OverflowMode="Never"/>
            </ToolBar>
        </ToolBarTray>
    </Grid>
</UserControl>
